{
  "rules": {
    "TPProd": {
      "UserData": {
        ".read": "true",
        "UserProfile": {
          "$userId": {
            ".read": "true",
            ".write": "auth != null && auth.uid == $userId && (auth != null && auth.uid == $userId)"
          }
        },
        "OtherProfiles": {
          "$userId": {
            ".read": "true",
            ".write": "auth != null && auth.uid == $userId"
          }
        },
        "UserInterests": {
          "$userId": {
            ".read": "true",
            ".write": "auth != null && auth.uid == $userId"
          }
        }
      },
      "Meetings": {
        ".read": "true",
        "MeetingAttendanceByUser": {
          "$meetingId": {
            "$userId": {
              ".read": "true",
              ".write": "auth != null && auth.uid == $userId"
            }
          }
        }
      }
    }
  }
}
